vectornav_imu_publisher:
  ros__parameters:
    # Serial port configuration
    port: "/dev/ttyUSB0"
    baud_rate: 460800

    # ROS2 configuration
    frame_id: "imu_link"
    publish_rate: 200.0  # Hz